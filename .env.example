# Copy to .env and fill real values. NEVER commit .env
VITE_API_USER=
VITE_API_PASS=
VITE_MAPBOX_TOKEN=
# Base URL for Geobox API. In dev defaults to /api (vite proxy). In production set full URL or /api behind nginx
# VITE_API_BASE=/api
